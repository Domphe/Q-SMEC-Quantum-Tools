# Validation Rules
- Timestamps must be ISO-8601 with timezone.
- Numeric fields must not be strings unless flagged in `notes`.
- Sources must be official or well-established repositories.
- Any claim must map to one or more `records` rows.
